spring.application.name=transactions

# El mapeo de parent_id <-> parentId se hace de forma explícita con @JsonProperty
# en TransactionRequest, para que el contrato JSON y el schema OpenAPI coincidan.
# Rechazamos cuerpos con campos desconocidos en vez de ignorarlos en silencio.
spring.jackson.deserialization.fail-on-unknown-properties=true
