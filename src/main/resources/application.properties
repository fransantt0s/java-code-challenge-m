spring.application.name=transactions

# El body de la API usa snake_case (parent_id); los records usan camelCase (parentId).
# Esta estrategia mapea ambos sin anotaciones por campo.
spring.jackson.property-naming-strategy=SNAKE_CASE
